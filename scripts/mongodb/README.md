- Install mongodb

- Setup data: users,...
    ```
    mongo <script.js>
    ```

- Enable authorization in /etc/mongod.conf
    ```
    security:
        authorization: enabled
    ```

- Mongo restart/reload
